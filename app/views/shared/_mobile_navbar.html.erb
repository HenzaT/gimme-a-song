<div class="navbar navbar-expand-sm navbar-light navbar-lewagon d-block d-sm-none">
  <div class="container-fluid">
    <%= link_to root_path, class: "navbar-brand" do %>
      Gimme a Song
    <% end %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto">
        <% if user_signed_in? %>
          <li>
            <a class="nav-link" href="<%= root_path %>">
              Home
            </a>
          </li>
          <li>
            <a class="nav-link" href="<%= new_song_path %>">
              New Idea
            </a>
          </li>
          <li>
            <a class="nav-link" href="<%= my_songs_path %>">
              My Songs
            </a>
          </li>
          <li class="nav-item">
            <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "nav-link" %>
          </li>
        <% else %>
          <li>
            <a class="nav-link" href="<%= new_song_path %>">
              New Idea
            </a>
          </li>
          <li>
            <a class="nav-link" href="<%= new_user_session_path %>">
              Login
            </a>
          </li>
          <li>
            <a class="nav-link" href="<%= new_user_registration_path %>">
              Sign up
            </a>
          </li>
          <li class="nav-item">
            <%= link_to "Log out", destroy_user_session_path, data: {turbo_method: :delete}, class: "nav-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
